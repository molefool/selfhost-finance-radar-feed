# Selfhost Finance Radar Feed

Static intelligence radar feed published for `selfhost-finance-app`.

The finance app reads this repository's GitHub Pages JSON feed. Collection logic stays outside the private app repository.
